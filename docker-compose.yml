services:
  dashboard:
    build:
      context: ./dashboard
    ports:
      - "8501:8501"
    volumes:
      - ./dashboard:/app
    working_dir: /app
    command: streamlit run main.py --server.port=8501 --server.address=0.0.0.0